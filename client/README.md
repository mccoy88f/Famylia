# famylia_client

Codice client generato da Serverpod. Dopo ogni modifica a modelli o endpoint nel server:

```bash
cd server
serverpod generate
```

Il client verrà aggiornato automaticamente in questa directory.
